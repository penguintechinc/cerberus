apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cerberus-beta
namePrefix: beta-
commonLabels:
  environment: beta
resources:
  - ../../base
replicas:
  - name: flask-backend
    count: 2
images:
  - name: registry-dal2.penguintech.io/cerberus/flask-backend
    newTag: latest
configMapGenerator:
  - name: cerberus-config
    behavior: merge
    literals:
      - LOG_LEVEL=INFO
      - RELEASE_MODE=false
