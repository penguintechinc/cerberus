# Cerberus IPS - Threshold Configuration
# Suppress noisy alerts and rate limit frequent signatures

# Example: Suppress specific SID from specific IP
# suppress gen_id 1, sig_id 2100498, track by_src, ip 192.168.1.1

# Example: Rate limit alerts (max 10 per 60 seconds)
# rate_filter gen_id 1, sig_id 2100498, track by_src, count 10, seconds 60, new_action drop, timeout 60

# Suppress common false positives
# suppress gen_id 1, sig_id 2013028
# suppress gen_id 1, sig_id 2013031
